name: Deployment Workflow
on:
    push:
        branches: [master]

jobs:
    job_one:
        name: Deploy
        runs-on: ubuntu-18.04

        steps: 
        - name: probando ip server ssh conexion
          uses: KatherineXRios/nodegit@master
          with:
            key: ${{ secrets.NUEVO_SECRET }}
            username: developer
            host: ${{ secrets.HOST_SECRET }}         
            port: 3000
            script: |
              cd nodegit
              git pull origin master
              git status
